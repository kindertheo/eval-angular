import { LimitPipePipe } from './limit-pipe.pipe';

describe('LimitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LimitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
