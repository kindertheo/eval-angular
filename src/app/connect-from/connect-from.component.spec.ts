/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ConnectFromComponent } from './connect-from.component';

describe('ConnectFromComponent', () => {
  let component: ConnectFromComponent;
  let fixture: ComponentFixture<ConnectFromComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ConnectFromComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ConnectFromComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
