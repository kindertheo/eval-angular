/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TodoLigComponent } from './todo-lig.component';

describe('TodoLigComponent', () => {
  let component: TodoLigComponent;
  let fixture: ComponentFixture<TodoLigComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TodoLigComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TodoLigComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
